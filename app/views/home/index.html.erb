<div class="col-md-4">
  <h1>Top Movies</h1>

  <% @movies.each do |movie| %>
    <h2>
      <%= movie.name %> Ranked: <%= movie.votes %><br>
    </h2>
  <% end %>
</div>

<div class="col-md-4">
  <h1>Top Books</h1>

  <% @books.each do |book| %>
    <h2>
      <%= book.name %> Ranked: <%= book.votes %><br>
    </h2>
  <% end %>
</div>

<div class="col-md-4">
  <h1>Top Albums</h1>

  <% @albums.each do |album| %>
    <h2>
      <%= album.name %> Ranked: <%= album.votes %><br>
    </h2>
  <% end %>

</div>
