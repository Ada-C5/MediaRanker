<h1> Add Movie</h1>

<%= form_for @movie, method: :post, url: create_movie_path(@movie.id), :html => { :class => "form-horizontal center" } do |f| %>
  <%=render partial: 'form' , :locals => { :f => f } %>
  <%= f.submit class: 'btn btn-success'  %>
<% end %>
