<h2>All Books</h2>

<table>
  <tr>
    <td>Votes</td>
    <td>Title</td>
  </tr>
  <% @books.each do |book| %>
  <tr>
    <td><%= book.votes %></td>
    <td><%= link_to book.name, book_path(book.id) %></td>
  </tr>
  <% end %>
</table>

<%= link_to "Add New Book", new_book_path%>
