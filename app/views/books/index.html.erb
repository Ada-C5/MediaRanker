<div class= "col-lg-12">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Ranking</th>
        <th>Name</th>
        <th>Upvote</th>
      </tr>
    </thead>
    <tbody>
      <%@books.each do |book|%>
        <tr>
          <td>Ranked: <%= book.upvote%></td>
          <td><%=link_to book.name, book_path(book.id)%></td>
          <td><%=link_to "Upvote", book_upvote_path(book), class: "btn btn-default", method: :put%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

</form>