<h1> Edit Book</h1>

<%= form_for @book, method: :patch, url: update_book_path(@book.id), :html => { :class => "form-horizontal center" } do |f| %>
  <%= render partial: 'form' , :locals => { :f => f } %>
  <%= f.submit class: 'btn btn-success'  %> 
<% end %>
